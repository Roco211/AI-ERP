# 销售 v0.8 验收清单

S0–S5 已实施并通过验收。本地最终全量回归、交付材料及代码提交的 push / PR CI 均通过；最终文档提交 SHA 及其再次运行的 CI 由仓库外 verification 记录，交付前再次核验。[施工规范](sales-v0.8.md) · [ADR0014](adr/0014-sales-reservations-returns-and-margin.md)。

## A 基线与架构

- [x] A01 以发布main64bec0a、0008_purchasing为基线；规范、ADR S1–S8、阶段边界及增量明确。
- [x] A02 冻结栈与原文约束已核对；已明确当前引擎跨凭据占用消费缺口，未改写旧价格单位语义。
- [x] A03 新表FORCE RLS/租户复合外键/forge_app；跨租户读写、缺上下文及连接池隔离拒绝。
- [x] A04 只有InventoryEngine写余额/占用投影；流水不可改删；普通库存及采购Command拒绝销售类型。
- [x] A05 内部占用凭据不可公开创建/改写/冲销；出库来源与占用来源均可追溯，对账可重建。

## B 订单与占用

- [x] B01 草稿零库存影响；确认100使reserved+100、on_hand/价值不变，可用量相应减少。
- [x] B02 多行确认任一缺货全部回滚；缺货保持DRAFT，不产生部分占用。
- [x] B03 版本冲突、失效资料、换算变化被拒绝；确认后商业内容和行在API/SQL均不可改删/追加。
- [x] B04 可用10时两订单并发确认7仅一个成功；不同库存键可并行。
- [x] B05 取消仅无有效出库；关闭释放未发占用；资料后来停用不阻止合法释放；重复操作不二次释放。
- [x] B06 关闭与出库并发结果完整一致，无悬空占用；全发不自动关闭，CLOSED不自动重开。
- [x] B07 履约状态与业务状态分离；退货不重开待发，CLOSED可执行出库量=0。

## C 出库与库存成本

- [x] C01 一次及分批出库正确减少on_hand和本单reservation；累计不能超发/超占用。
- [x] C02 出库不能消费其他订单/仓库/商品/租户占用；伪造reservation字段输入被拒绝。
- [x] C03 确认后采购改变均价，出库取实际当时成本；历史毛利不随以后均价变化。
- [x] C04 2箱×1000快照准确；确认后因子变更不改后续出库；不可表示基本数量明确拒绝。
- [x] C05 同单不同键并发POST仅一次生效；原键回放、异body冲突、过期幂等和撤权不绕过状态。
- [x] C06 六处故障（流水/余额/单据/Audit/Outbox/幂等）、第二行及COMMIT故障全部回滚，原键可重试。
- [x] C07 真实HTTP成功返回前事务已提交；立即查询新单和状态可见；提交失败返回Problem Details。
- [x] C08 RESERVE→ISSUE→RELEASE及冲销后余额与reservation均能从流水重建；不改变旧库存对账行为。

## D 退货与冲销

- [x] D01 退货仅原POSTED出库、原客户/仓库/单位；跨来源/租户/超量拒绝，关闭订单允许合法退货。
- [x] D02 当前均价变化后，退货仍回收原出库实际成本；入库价值正确参与当前移动均价。
- [x] D03 原价销售冲减与原实际成本回收分别记录；最后退完各自清零，部分舍入溢出明确报错。
- [x] D04 并发退货不超过原实发；退货不恢复履约/占用，原成本为零也正确。
- [x] D05 出库冲销恢复原库存/价值/均价及原占用；退货冲销恢复退货额度；原事实保留、重复冲销拒绝。
- [x] D06 后续流水/有效退货依赖拒绝出库冲销；CLOSED出库冲销拒绝；不支持部分/级联冲销。

## E 定价、毛利与权限

- [x] E01 专属→有效成交历史→客户等级→标准的每级优先级、缺价、零价及跨租户过滤通过。
- [x] E02 历史排除草稿/确认单/冲销出库；退货不形成新成交价；全部退回的未冲销成交按规范保留。
- [x] E03 目录基本单位价与历史快照正确换算；只舍入最终单位价；正报价舍入为零冲突；来源可追溯。
- [x] E04 AUTO保存时解析，MANUAL显式输入留审计；确认不重报价；更换客户报价重核，服务端不信任客户端来源。
- [x] E05 不接受float、NaN、Infinity、超过6位/范围金额；行金额4位、单据合计准确；库存全出成本尾差、原出库行全退销售金额与成本尾差分别结清，拆单独立舍入边界明确。
- [x] E06 草稿/确认不计已实现毛利；出库60×15/成本660，退10后净销售750、净成本550、净毛利200。
- [x] E07 每个动作独立权限；无成本销售员可开单/确认，无价格仓管可按冻结来源出库/退货。
- [x] E08 无成本/价格权限时列表、详情、历史、汇总、错误、回执均不泄露成本或可反推成本的毛利。
- [x] E09 Audit实际前后值、actor/request ID；Outbox仅元数据；Redis/worker停止不影响核心过账，恢复消费不重复库存。

## F 工作台

- [x] F01 真实浏览器客户→键盘选品/单位/价格→草稿→确认占用→分批出库→退货→合法冲销→来源追溯通过。
- [x] F02 订单显示已发/未发/已退/剩余占用及关闭后的可执行0；价格来源和服务端金额清楚。
- [x] F03 加载、无数据、权限拒绝、版本/库存/价格冲突、乱序详情、重复点击、响应丢失原键原body重试有回归。
- [x] F04 真实无成本销售员和仅数量仓管账户完成允许动作、越权403；390px不整页横向溢出。
- [x] F05 所有列表服务端分页/有界筛选，200行上限；RHF/Zod/TanStack与生成DTO沿用，浏览器不算财务金额。

## G 交付

- [x] G01 空库及含采购/库存/价格/权限的0008数据升级新head通过；旧迁移及事实不改写。
- [x] G02 冻结安装、Ruff/format/Pyright、完整后端/Catalog/Inventory/Purchasing回归通过，零skipped冒充。
- [x] G03 前端lint/typecheck/Vitest/build/Playwright通过，OpenAPI→TS无漂移。
- [x] G04 sales/** push及PR完整CI生效；最终交付SHA的CI成功，记录run链接，不用旧采购CI替代。
- [x] G05 可选销售seed仅开发环境、走Command、重复保留已有事实；不加入真实凭据。
- [x] G06 实现摘要、完整目录、运行命令、迁移、测试、限制、待复核决策齐备；全部通过才建议sales-v0.8标签。

## S0检查记录

已核对原文销售占用/成本/单位/价格/状态要求，以及当前Engine.source/change/reverse、Catalog基础价单位、0005库存来源外键与0008采购基线。已定义46项验收，其中2项仅针对规格准备；余项必须在施工时由真实测试补证据。

本规格没有实现销售业务，没有新增数据库迁移，没有修改已发布purchasing-v0.7。文档PR的CI即使通过，也只表示既有代码回归，不能勾选待施工项。

## S1证据与未完成部分

- `test_sales_orders.py`：200件均价11，确认100件后现有200/占用100/可用100/价值2200；释放后占用为0并对账一致。多行缺货全回滚、竞争库存仅一成功、独立仓库不受另一键等待阻塞、历史停用资料可释放、RLS和不可变来源通过。
- `test_sales_pricing.py`：目录各级价、零价、AUTO保存与确认冻结、单位转换、手动价和非法输入通过。有效历史优先级/草稿排除/冲销排除使用明确的查询fixture；全退真实成交语义仍等待S3。
- `test_sales_safety.py`、`test_api_commit_boundary.py`：八处故障含COMMIT回滚与原键重试、撤权后幂等拒绝、真实HTTP提交边界、Outbox重复消费不二次占用通过。
- `test_migrations.py`：空库与0008包含采购订单、库存流水、价格和角色数据升级0009通过；旧事实保持，四张新表FORCE RLS。
- 本次完整后端164 passed / 0 skipped。阶段CI、前端与限制见[交付记录](sales-v0.8-s1-delivery.md)。

46项中7项完整勾选。A03/B03/B05/B07/E01/E03/E04/E07/E08/E09已有S1部分证据，仍保留未勾选，等待出库/退货路径或UI验证；其余S2–S5项目未完成。不建议销售版本标签。


## S2证据与未完成部分

- `test_sales_shipments.py`：一次/分批出库60+40、原订单占用消费、1000因子冻结、采购后实际均价与全出尾差、关闭/取消/出库竞争、混合采购及普通库存扣减、资料停用、草稿版本和不可变保护。
- `test_sales_shipment_safety.py`：权限与数量角色、跨租户和输入字段拒绝、SQL过账后保护、八处故障含COMMIT以及第二行失败回滚、原键重试、整单金额上限、Audit/Outbox/Redis独立性、真实HTTP提交边界。
- `test_sales_reservation_sources.py`：独立来源消费后对账重建、无占用/空链接/伪造链接/其他订单/调用数量与operation篡改/错误能力拒绝、延迟SQL关联约束、原库存同行限制保留。
- `test_sales_read_consistency.py`：状态筛选后并发关闭/过账，不返回失配的订单或出库单。
- `test_migrations.py`新增含S1确认订单、价格快照、占用凭据/投影/流水的0009升级0010；空库及旧基线继续回归。`test_catalog.py`的ConversionSnapshot精度拒绝测试与S2冻结换算测试共同覆盖C04。

当前46项中17项完整勾选。C03的历史实际成本已通过，但毛利待S3；C05已有并发/回放/撤权测试，完整过期幂等矩阵待最终验收；C08的占用→出库→释放对账已通过，销售冲销重放待S3。B07、D组、E组及F/G剩余项保持待办，不把后端S2或既有页面CI当成销售全流程通过。

最新结果和完整目录见[交付记录](sales-v0.8-s2-delivery.md)。


## S3 证据与未完成部分

- `test_sales_returns.py`、`test_sales_return_safety.py`：43 项真实业务/安全测试，覆盖按原出库成本入库、原单位快照、销售金额/成本尾差独立清零、部分舍入冲突、显式刷新过期分配、超退竞争、末笔冲销、闭单规则、资料停用后的合法冲销、全事务回滚和原键重试。新增出库冲销与关闭竞争验证关闭后占用始终为零。
- `test_sales_return_sources.py`：25 项来源及 Engine 边界测试，覆盖租户外键、SQL 延迟关联、错误能力、无来源/伪造来源/金额、真实原流水和冲销凭据核验、恢复原占用、逆向重放与对账。既有库存/采购冲销回归继续通过。
- `test_sales_margin_history.py`：16 项毛利/历史测试，包含 900/660/240 → 退货后 750/550/200、后续采购不改历史毛利、负毛利、全退仍保留成交价、实际成交优先于客户等级价、当前资料变更不改快照、极小正报价精度冲突、跨租户、分页和并发复核；所有价格/成本权限组合按接口检查。
- 退货安全测试校验 Audit 实际前后值、`actor_id` 与认证用户一致、`request_id` 与 HTTP 请求/响应一致；Outbox 仅元数据，Redis 故障及重复消费不重复库存。
- `test_migrations.py`：9 条空库/旧版本升级路径，新增 0010 已过账出库、占用消费和实际 ISSUE 成本保留验证；旧迁移不改写。`test_api_commit_boundary.py` 增加销售冲销的真实成功响应提交边界。

本轮清单为 **34/46**。C05 的出库过期幂等完整矩阵和 E04 的界面更换客户/报价复核继续待办；F 组工作台、G 组最终交付在 S4/S5 完成后逐项验收。S3 的退货/冲销过期记录及撤权场景已有回归，不把它替代所有出库路径。完整回归和当前提交 CI 结果见[本轮交付记录](sales-v0.8-s3-delivery.md)及 verification 附件。


## S4 证据与未完成部分

- `test_sales_workspace_queries.py` 新增 11 项真实数据库测试：冻结销售单位的待发/可执行量、闭单与退货额度、库存三值同一快照、缺余额归零、不同权限组合、其他订单并发改变库存以及历史原单关联。原写入命令、Engine 与迁移均未修改。
- `tests/sales.test.tsx` 共 17 项工作台回归：加载/空数据/拒绝、不同价格与成本权限、切换客户清价、过期报价和详情、保存与业务冲突、重复点击、失联后原键原 body 重试、重试又遇 403 时保留提交、撤权后缓存价格隐藏、退货草稿成本回收报价。
- `picker.test.tsx` 6 项和 `inventory.test.tsx` 7 项通过；新增覆盖迟到换算、重复 Enter、离开组件后的响应、按原凭据筛选库存流水，以及占用/出库/退货跳回销售原单。
- `e2e/sales.spec.ts` 3 个隔离企业真实浏览器场景通过：客户→键盘/包装单位→AUTO 价格 150→保存→确认 100 基本单位→发 60→退 10→退货冲销→历史/库存来源跳转。真实响应丢失后的两次请求键与 body 完全相同，仅一条 ISSUE；净毛利 240→200→240。无成本销售员与无价格仓管完成各自操作，越权 HTTP 403、敏感字段过滤及 390px 无整页溢出通过。
- 完整后端 **316 passed / 0 skipped**，完整前端 Vitest **37 passed**；静态检查、生产构建及生成契约无漂移通过。既有浏览器回归及当前提交 CI 见本轮交付记录和 verification 附件。

清单现为 **40/46**。C05 与 G02–G06 留待 S5 完整阶段验收；G 组不会因 S4 一次回归通过就提前勾选。没有新增销售开发 seed、没有合并或发布 PR #4，不建议 `sales-v0.8` 标签。


## S5 专项证据与最终验收状态

- `test_sales_idempotency.py`：21 项真实 API 测试通过，覆盖同键/异键并发过账、历史回执与异 body 冲突、过期及清理记录后的 POSTED/REVERSED 状态保护、撤权、失效认证、会话/操作者/租户隔离以及不同单据和操作的键范围。C05 据此完成。
- `test_sales_search.py`：3 项真实 API 测试通过。订单与出库查询把反斜杠、百分号、下划线当作字面客户文本，不误匹配其他记录；分页和跨租户结果正确。
- `test_sales_rounding.py`：1 项真实采购→确认→三次出库→逐原单全部退回测试通过。订单金额 `0.0002`、三个独立出库行销售金额累计 `0.0003`；实际成本 `0.3333 + 0.3333 + 0.3334 = 1.0000`，库存全部出完后价值为零；按各原出库行全部退回后净销售、净成本、净毛利均为零。该证据明确既有计价规则，不要求拆单累计销售金额强制等于订单金额。
- `test_sales_seed.py`：16 项测试通过，覆盖仅开发环境、真实操作者权限、forge_app/RLS、所有写入经 Command、同一事务及并发锁、失败整体回滚、幂等记录过期后不重复、资料改名/关闭/退货冲销后保留，以及单独伪造审计标记不充当业务完成证据。
- 真实 DEMO 演示连续运行返回 `created`、`preserved`，订单为 `8dad4947-c498-474d-aa74-02b035a594ee`；现有/占用/可用库存 `150 / 40 / 110`，净销售 `750`、净成本 `550`、净毛利 `200`。第二次保留已有事实，G05 据此完成。

当前 **46/46** 项已完成。下列证据来自 S5 最终验证，不使用 S4 的全绿结果替代；文档提交后的最终 SHA 和两条 CI 在交付前再次核验并记录于仓库外 verification。

| 验收 | 最终证据状态 |
|---|---|
| G02 冻结安装、静态检查、完整后端回归 | 已通过：357 passed / 0 skipped，399.39 秒；最终版本元数据更新后平台专项 11 passed，4.78 秒；冻结安装、Ruff、111 文件格式检查、Pyright 0 错误/0 警告通过 |
| G03 前端检查、构建、浏览器、契约无漂移 | 已通过：完整 Vitest 44 passed / 6 文件，42.63 秒，其中销售 17 项、导航保护 7 项；lint、typecheck、生产 build、OpenAPI→TS 无漂移通过；真实生产浏览器无诊断注入运行 11/11 通过，约 1 分钟，含主链、独立角色与 Back/Forward 重试保护 |
| G04 最终交付 SHA 的 push / PR CI | 已通过：代码提交 `9d7961e` 的 [push CI](https://github.com/Roco211/AI-ERP/actions/runs/33977824715) 与 [PR CI](https://github.com/Roco211/AI-ERP/actions/runs/33977827345) 均成功；最终文档提交 SHA 及再次运行的 CI 见仓库外 verification |
| G06 最终交付说明、目录、运行/迁移与限制 | 已完成：[最终交付记录](sales-v0.8-delivery.md)包含实现、运行、迁移、测试和边界，[完整目录](sales-v0.8-tree.txt)包含 230 个跟踪文件；建议评审合并后创建 `sales-v0.8` 标签 |

待确认提交不持久化到本地存储。认证失效、显式退出登录、刷新或浏览器关闭/崩溃后，应先核对服务端事实，再开始新的写入；页面导航保护不代表跨重启恢复。遇到无法识别位置的旧浏览历史条目时，回退保护只保留待确认页面，不能恢复被替换的旧网址。PR #4 保持草稿，当前未合并、创建销售标签或发布销售版本。建议评审合并后创建 `sales-v0.8` 标签，本轮不执行发布。

数据库 head 保持 `0011_sales_returns`，本轮不追加迁移；应用版本元数据已更新为 `0.8.0`。最终材料见[交付记录](sales-v0.8-delivery.md)，最终 SHA 和最新 CI 证据由仓库外 `Forge-ERP-Sales-v0.8-verification.json` 记录。
