# 运营 v0.10 验收清单

状态：仅有规范准备，业务实现尚未开始，以下 **42 项全部待验收**。必须先完成资金 v0.9，再按 O1–O5 逐项提供证据；不把计划、模拟输出、旧版本CI或 skipped 当作通过。

[施工规范](operations-v0.10.md) · [ADR0016](adr/0016-catalog-import-execution.md) · [资金前置阶段](funds-v0.9-acceptance.md)

## A 基线、权限与事实

- [ ] A01 v0.9全部验收并记录实施基线SHA/实际head；O0–O5范围明确，没有提前实现AI或更换基础设施。
- [ ] A02 新表FORCE RLS/租户复合外键/非特权forge_app；API、worker、列表、结果下载的跨租户/缺上下文测试通过。
- [ ] A03 新权限按操作与资源叠加；预览/确认/重试/后台每行/缓存回执/下载均检验当前权限，不接受文件租户或执行身份。
- [ ] A04 Catalog导入和补货生成采购只调用现有Command；没有直接写库存/资金事实，原单位、价格、状态和成本语义不变。
- [ ] A05 所有相关HTTP成功在COMMIT后返回；request ID、Problem Details、结构化日志、Audit/Outbox不泄露文件正文或敏感单元格。

## B 文件、预览与10资源覆盖

- [ ] B01 10种Catalog资源各有可用xlsx模板、字段说明、定位键及真实导入案例；每批只执行用户选定的一种资源/工作表。
- [ ] B02 10MB/10,000行/64列和实际ZIP解压字节/成员数/压缩比限制测试通过，伪造dimension不能绕过边界。
- [ ] B03 宏、公式/缓存公式、加密、外部链接、嵌入对象、危险/重复路径、DTD/实体和损坏xlsx被拒绝，无业务写入。
- [ ] B04 文本前导零条码完整保留；数字编码、科学计数/日期/自定义数字显示格式报UNSAFE_IDENTIFIER_CELL，不猜回编码。
- [ ] B05 数量/金额/换算从XML原文构造Decimal；数值极限、负值、NaN/Infinity、超精度、空值与显式零区分正确。
- [ ] B06 原值与清洗值分列，空白/全角空格/乘号有解释；编码不改大小写，不自动合并近似名称。
- [ ] B07 未知/重复列和organization_id/user/id/version/status/search_text等禁用字段拒绝；未选工作表不自动执行且仍接受安全检查。
- [ ] B08 当前组织编码引用、active状态、分类属性必填/未知键/枚举/数值/布尔均与现有API一致；同文件新增引用给出分批处理路径。
- [ ] B09 文件内重复自然键/条码/供应商货号/价格键全部报错，默认现有编码不覆盖，不能最后一行获胜。
- [ ] B10 显式更新重新预览并冻结expected_version；未映射可选字段不被默认值清空，基础单位/关联不变量和原历史快照不改变。
- [ ] B11 模板/预览/错误结果导出把以=、+、-、@开头的用户文本写为文本，Excel公式注入案例通过。
- [ ] B12 预览分页展示原值/清洗值/动作/行号/中文错误；任一预览错误禁止确认，当前权限不足不能读回价格或联系资料。

## C 逐行执行、恢复与保留

- [ ] C01 每行Catalog业务、成功状态、目标ID、Audit/Outbox同事务；注入业务写/基础换算/Audit/Outbox/行结果/幂等/COMMIT故障全部回滚该行。
- [ ] C02 两worker、重复消息、重复确认/重试和同键异body不能重复行；并发执行和批次统计锁序不死锁，统计与持久行状态一致。
- [ ] C03 中途终止worker/Redis后可恢复；10,000行分批执行不过长事务，embedding关闭或其Outbox队头阻塞不阻止导入。
- [ ] C04 预览后目标/引用/分类模板修改、停用、撤权和账号停用均重新检查；失败不静默刷新版本或改写冻结内容。
- [ ] C05 成功行在幂等TTL过期、记录被清理、COMMIT响应丢失后仍不重做；错误记录不能覆盖并发已成功状态。
- [ ] C06 UI只重试失败行；成功行不可选，未执行行由原批次恢复；旧版本业务冲突必须新预览，部分完成和原业务来源清楚可见。
- [ ] C07 请求后原xlsx临时文件释放；7天后清理原值/清洗值/错误正文并禁止执行，清理与运行并发安全，成功状态/目标/审计永久保留。
- [ ] C08 真实浏览器完成上传→预览→确认→部分失败→仅失败重试→刷新恢复→来源查看/错误下载，断网及双击不二次创建资料。

## D 补货公式与采购草稿

- [ ] D01 组织汇总口径与原低库存保持一致；无余额为0，V=现有-占用；调拨、内部占用凭据、其他租户不污染销量/在途。
- [ ] D02 30个完整业务日/时区/半开区间正确；退货按自身时间扣减，REVERSED排除；N负值、净零和无历史清晰区分。
- [ ] D03 CONFIRMED采购订购减有效实收得到O；100收25退10仍待收75；DRAFT/CLOSED/CANCELLED及已冲销收货处理正确。
- [ ] D04 规范全部7组数值例通过，包括20建议量、在途覆盖归零、阈值边界、静态补货、无依据不补货和0.266667向上精度；无float及中间提前舍入。
- [ ] D05 缺提前期明确静态分支，显式lead_days=0与未配置不同；days_of_stock零除保护、上限/非法值拒绝，无猜箱规/MOQ。
- [ ] D06 一致快照/并发销售或采购变动后要求重新复核；用户选供应商/仓/单位/价格后仅生成单供应商单仓DRAFT，金额与换算由原Command确定。
- [ ] D07 生成草稿的原键重试、并发、响应丢失及幂等过期不重复建单；无成本用户可按授权看数量建议，越权建单403，库存/资金保持不变。

## E 经营概览与日期口径

- [ ] E01 日期窗口的有效出库/退货净额、实际成本、毛利与来源明细相等；草稿/确认不算成交，缺实际成本不按0造毛利。
- [ ] E02 期间收/付/退款按v0.9业务日期及有效事实计算，期初/历史映射不冒充现金；销售净额不被标作已收款或会计利润。
- [ ] E03 当前应收应付/待退款/库存估值和候选数量显示as_of，不随日期筛选假冒历史余额；资金未启用/来源不完整明确说明。
- [ ] E04 dashboard.read不提升底层权限；所有金额/成本/资金/趋势/缓存/钻取组合不泄漏，跨租户拒绝；当前有效事实冲销后的追溯重算说明一致。
- [ ] E05 有界聚合与同一数据库快照通过并发测试；没有N+1/联表乘倍计数，日期边界/时区/366日上限/无数据和真实来源钻取通过。

## F 回归与交付

- [ ] F01 空库和含已验收v0.9历史数据升级成功，旧迁移/库存/资金/历史快照保持；新增RLS、权限、worker最小发现能力经真实PG18验证。
- [ ] F02 uv/pnpm冻结安装、Ruff/format/Pyright、全后端含Catalog/库存/采购/销售/资金回归通过，零skipped冒充。
- [ ] F03 OpenAPI→TypeScript无漂移；前端lint/typecheck/Vitest/build及正式生产浏览器通过，390px无整页横向溢出。
- [ ] F04 最终交付SHA的push/PR CI成功且有链接；独立测试租户清理完毕，不把临时诊断注入或旧版本CI当作最终证据。
- [ ] F05 交付摘要/目录/运行恢复清理命令/迁移/测试/限制/架构决策齐备；全部通过后才推荐operations-v0.10，不自动合并、打标签或发布。

## O0 记录

已只读核对Catalog service/RESOURCES/schema/rules、现有库存组织级low_stock、采购有效收货与待到口径、销售实际成本与当前POSTED毛利口径、worker向量事件处理边界及资金v0.9规范。已写正式施工规范和ADR；没有新增业务实现或测试通过证据，所以本清单仍为0/42。
